{
  "name": "@blockr/blockr-validator",
  "version": "2.0.0",
  "description": "Blockr validator package",
  "main": "dist/server.js",
  "types": "dist/server.d.ts",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "build-watch": "tsc -w -p tsconfig.json",
    "test": "echo \"Error: no test specified\" && exit 1",
    "lint": "tslint --project tsconfig.json",
    "tsc": "tsc --listFiles"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/ESD6-blockr/blockr-validator.git"
  },
  "author": "Blockr",
  "license": "MIT",
  "bugs": {
    "url": "https://github.com/OrdinaBlockchain/blockchain-validator/issues"
  },
  "dependencies": {
    "@blockr/blockr-data-access": "^1.1.0-10",
    "@blockr/blockr-models": "^1.2.0-9",
    "@sentry/node": "^4.6.6",
    "inversify": "^5.0.1",
    "winston": "^3.2.1"
  },
  "devDependencies": {
    "@types/jest": "^24.0.11",
    "@types/mongodb": "^3.1.23",
    "jest": "^24.7.1",
    "ts-jest": "^24.0.2",
    "tslint": "^5.14.0",
    "tsutils": "^3.9.1",
    "typescript": "^3.3.4000"
  },
  "jest": {
    "moduleFileExtensions": [
      "ts",
      "tsx",
      "js"
    ],
    "transform": {
      "^.+\\.(ts|tsx)$": "ts-jest"
    },
    "globals": {
      "ts-jest": {
        "tsConfigFile": "tsconfig.json"
      }
    },
    "testMatch": [
      "**/__tests__/*.+(ts|tsx|js)"
    ]
  }
}
